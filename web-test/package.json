{
  "private": true,
  "scripts": {
    "test": "wasm-pack --version",
    "build:wasm": "wasm-pack build --release --target web --out-dir ../web-test/pkg ../raytracer-wasm",
    "build:web": "webpack build ./index.js --mode production -o dist --output-filename index.js && shx cp -r index.html serve.json scenes textures dist/",
    "build": "npm run build:wasm && npm run build:web",
    "serve": "serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "shx": "^0.3.4",
    "wasm-pack": "^0.12.1",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "wasm-feature-detect": "^1.8.0"
  }
}
