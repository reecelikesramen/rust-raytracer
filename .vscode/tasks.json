{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Release",
      "type": "shell",
      "command": "cargo build -p raytracer-cli --release && mv target/release/raytracer-cli raytracer",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$rustc"],
      "detail": "Builds the project in debug mode"
    }
  ]
}
